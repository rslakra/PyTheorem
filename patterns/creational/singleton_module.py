# Create a file named `singleton_module.py`
# singleton_module.py

class SingletonModule:

    def __init__(self):
        self.data = "Initial data"


# Create a single instance within the module
instance = SingletonModule()
